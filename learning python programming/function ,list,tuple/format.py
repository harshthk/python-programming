Number = int(input("Enter the decimal number:-"))

print(bin(Number),"Number is now converted into binary")
print(hex(Number),"Number is now  converted into decimal")
print("Number is converted into binary:-",bin(Number))
print("Number is converted into headecimal:-",hex(Number))
